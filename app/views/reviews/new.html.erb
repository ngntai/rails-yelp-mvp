<div class="container">
  <h1><%= @restaurant.name %></h1>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :rating %>
    <%= f.input :content %>
    <%= f.submit "Envoyer", class: "btn btn-primary" %>
  <% end %>
</div>
